{
  "name": "htmplt",
  "version": "1.0.0",
  "dependencies": {
    "chokidar": "^3.5.3"
  },
  "main": "./dist/cli.ts",
  "scripts": {
    "build": "npx tsc",
    "buildlink": "npx tsc && npm link",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "zyxwars",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "typescript": "^4.9.4"
  },
  "files": [
    "dist/**/*"
  ],
  "bin": {
    "htmplt": "./dist/cli.js"
  }
}
