{
  "name": "htmplt",
  "version": "1.0.0",
  "dependencies": {
    "argparse": "^2.0.1",
    "chokidar": "^3.5.3"
  },
  "main": "./dist/cli.ts",
  "scripts": {
    "build": "npx tsc",
    "buildlink": "npx tsc && npm link",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "zyxwars",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^4.9.4"
  },
  "files": [
    "dist/**/*"
  ],
  "bin": {
    "htmplt": "./dist/cli.js"
  }
}
